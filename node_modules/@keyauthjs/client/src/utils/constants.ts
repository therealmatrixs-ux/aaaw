// The base URL for KeyAuth API
export const BASE_URL = "https://keyauth.win/api/1.2/";

// Common HTTP headers used in KeyAuth API requests
export const HEADERS = {
    accept: "application/json", // Specify that the response should be in JSON format
    Host: "keyauth.win", // Specify the host for the API requests
    "Content-Type": "application/x-www-form-urlencoded", // Specify the content type of the request body
};
